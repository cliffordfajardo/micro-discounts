/**
 * @description
 * Simple function that's only purpose it to help with debugging in VSCode.
 *
 * @example
 * Typical use case:
 *  1. put a break point inside the body of this function
 *  2. call `debug()` where you want to catch the debug in your code
 */
export function debug(){
  return;
}
